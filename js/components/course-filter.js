function renderCourses(filter="all"){const grid=document.getElementById("course-grid");if(!grid)return;grid.innerHTML=courseData.filter(c=>filter==="all"||c.category===filter).map(c=>`<article class="course-card"><span class="tag">${c.category.replace("-", " ")}</span><h3>${c.title}</h3><p>${c.description}</p><div class="course-bottom"><span>${c.duration}</span><a href="${c.url}">View Program →</a></div></article>`).join("")}document.addEventListener("DOMContentLoaded",()=>{renderCourses();document.querySelectorAll(".filter").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".filter").forEach(b=>b.classList.remove("active"));btn.classList.add("active");renderCourses(btn.dataset.filter)}))});